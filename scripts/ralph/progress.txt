# Ralph Progress Log
Started: Sun Apr 19 23:23:07 ACST 2026
---

## Codebase Patterns
- Routes are created as factory functions: `export function fooRoutes(sessionManager): Hono<{Variables: AuthVariables}>` in `src/api/routes/foo.ts`, then mounted in `server.ts` with `app.route("/v1/sandboxes", fooRoutes(sessionManager))`
- Auth: all `/v1/*` routes use `authMiddleware` (already applied at app level) and ownership is checked with `checkOwnership(sessionManager, sandboxId, c.get("owner"))`
- just-bash tar requires GNU-style dash-prefixed flags: use `tar -xzf` NOT `tar xzf` (POSIX style without dash fails in just-bash with "You must specify one of -c, -r, -u, -x, or -t")
- InMemoryFs.writeFile accepts Uint8Array (binary data) — safe to write binary archives
- `mkdir -p` in just-bash must create `/tmp` before writing to `/tmp/...` via `fs.mkdir` API; InMemoryFs doesn't auto-create parent dirs for writeFile (the route must ensure parents exist)
- Tests use `InMemoryFs` + real just-bash `Bash` (no mocking needed for exec) — commands actually execute in the virtual FS
- Biome lint: use dot notation `body.archive` not bracket notation `body["archive"]` for object keys (enforced as error)

---

## 2026-04-20 - US-071
- What was implemented: POST /v1/sandboxes/:id/ingest route in src/api/routes/ingest.ts; accepts multipart form with `archive` (tar.gz File) and optional `basePath` (default `/home/user/project`); writes to /tmp/_ingest.tar.gz via fs.writeFile; extracts via bash.exec with `tar -xzf`; validates basePath for shell safety
- Files changed: src/api/routes/ingest.ts (new), src/api/__tests__/ingest.test.ts (new), src/api/server.ts (added ingestRoutes mount)
- **Learnings for future iterations:**
  - just-bash tar does NOT support POSIX-style flags without dash (`tar xzf`) — must use `tar -xzf`
  - InMemoryFs does not auto-create /tmp — must call `fs.mkdir('/tmp', {recursive:true})` before writing there
  - Hono `c.req.parseBody()` returns `BodyData` where file fields are `File` instances and text fields are strings — check with `instanceof File`
  - Biome enforces dot notation for string literal keys on object access — use `body.archive` not `body["archive"]`
  - Pre-existing flaky test: `concurrency.ordering.test.ts` S2/S3 may fail non-deterministically (race condition, not related to ingest)
---

## 2026-04-20 - US-072
- What was implemented: POST /v1/sandboxes/:id/ingest-files route added to src/api/routes/ingest.ts; Zod validation for { basePath, files: Record<string, string> } (base64 values); decodes base64 with Buffer.from(base64, 'base64'); creates parent dirs via fs.mkdir recursive; writes each file via fs.writeFile; returns { status: 'ok', fileCount }
- Files changed: src/api/routes/ingest.ts (added route + zod schema), src/api/__tests__/ingest.test.ts (added 3 tests for US-072)
- **Learnings for future iterations:**
  - Base64 decoding: `Buffer.from(base64Content, 'base64')` then `new Uint8Array(decoded)` for fs.writeFile
  - Parent dir extraction: `absPath.slice(0, absPath.lastIndexOf('/'))` — no need for path.dirname, avoids importing node:path
  - Zod `z.record(z.string(), z.string())` correctly validates Record<string, string> maps
---
